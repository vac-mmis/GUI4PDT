export type ModelFile = {
    name: string;
    content: string;
};

export type MaterialFile = {
    name: string;
    albedo: string;
    ao: string;
    metalness: string;
    normal: string;
    roughness: string;
};
